{
  "name": "reader-dev",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/anthropics/devcontainer-features/claude-code:1.0": {}
  },
  "remoteEnv": {
    "BRIDGE_DIR": "${containerWorkspaceFolder}/.bridge",
    "PATH": "${containerWorkspaceFolder}/scripts/container:${containerEnv:PATH}"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends jq && chmod +x scripts/container/bridge scripts/host/*.sh scripts/test/run-bridge-tests.sh scripts/test/mock-bin/*"
}
